# ===== DATABASE SETUP (Initial & Ongoing) =====
spring.datasource.url=jdbc:mysql://localhost:3306/spot_a_bird_db
spring.datasource.username=spotter
spring.datasource.password=12345

# ===== HIKARI CONNECTION POOL (Always active) =====
#spring.datasource.hikari.connection-timeout=20000  # 20 seconds
#spring.datasource.hikari.maximum-pool-size=5       # Adjust based on your server cores
#spring.datasource.hikari.idle-timeout=30000        # 30 seconds
#spring.datasource.hikari.max-lifetime=600000       # 10 minutes

# ===== HIBERNATE (Initial vs Ongoing) =====
# For initial table creation:
#spring.jpa.hibernate.ddl-auto=create
#spring.sql.init.mode=always

# After tables exist (production):
 spring.jpa.hibernate.ddl-auto=update

# Data initialization
spring.sql.init.mode=always
spring.datasource.initialization-mode=always

# For MySQL only
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect